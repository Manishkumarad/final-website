{
  "name": "nexus-ai-agency",
  "version": "1.0.0",
  "description": "AI-powered development agency website",
  "scripts": {
    "build": "mkdir -p public && cp public/index.html public/index.html || echo 'Static site ready'",
    "dev": "serve public -p 8000",
    "start": "serve public -p 8000"
  },
  "dependencies": {
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
